defaults:
  - _self_
  - wandb: private.yaml
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

# disable hydra outputs
hydra:
  output_subdir: null
  run:
    dir: .

name: "mocov3-cifar100-multi-linear-test"
pretrained_feature_extractor: trained_models/mocov3/g256yzz0/mocov3-ego4d-lrsweep1-g256yzz0-ep=0.ckpt
backbone:
  name: "resnet50"
pretrain_method: "mocov3"
data:
  dataset: cifar100    # change here for cifar100
  train_path: "/home/data"
  val_path: "/home/data"
  num_workers: 4
optimizer:
  name: "sgd"
  batch_size: 128
  lr: 0.1
  weight_decay: 0
  momentum: 0.9
scheduler:
  name: "warmup_cosine"
  warmup_start_lr: 0.00001
  warmup_epochs: 0.0
  max_epochs: 100
  interval: "epoch"
early_stopping:
  enabled: False
checkpoint:
  enabled: False
  dir: "trained_models"
  frequency: 1
auto_resume:
  enabled: True

# creates a linear classifier on top of frozen features for every learning rate in the grid
grid:
  enabled: True
  lr: [0.0001, 0.0005, 0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1.0, 2.0, 3.0]

# overwrite PL stuff
max_epochs: 5
devices: 2
sync_batchnorm: True
accelerator: "gpu"
strategy: "ddp"
precision: 32
log_every_n_steps: 50
